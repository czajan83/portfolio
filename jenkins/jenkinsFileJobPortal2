pipeline { 
    agent any 
    stages {
        stage('Setup') { 
            steps { 
    			dir('/git/portfolio/playwright') {
                	sh(script: 'npm install')
                	sh(script: 'npx playwright install') 
                }
            }
        }        
        stage('GetJobsList') { 
            steps {
       			dir('/git/portfolio/playwright') { 
                	sh(script: 'npx playwright test tests/jobListPortal2.spec.ts --project=chrome') 
                }
            }
        }
    }
}
